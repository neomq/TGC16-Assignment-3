{{#extends 'base'}}

{{#block 'content'}}
<div class="container col-6 mt-4">
    <h1>Register</h1>
    <form method="POST">
        <input type="hidden" name="_csrf" value="{{csrfToken}}" />
        {{{form}}}
        <input type="submit" value="Register" class="btn mt-3 btn-primary" />
        {{/block}}
    </form>
</div>
{{/extends}}